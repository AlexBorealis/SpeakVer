[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]  # Проверять ошибки Pyflakes (F), PEP 8 (E), сортировку импортов (I)

[project.optional-dependencies]
dev = [
    "commitizen",
    "ruff",
    "black",
    "pytest",
]